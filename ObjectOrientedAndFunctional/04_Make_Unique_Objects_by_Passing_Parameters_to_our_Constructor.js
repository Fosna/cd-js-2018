// Create Person class. Person should have firstName, lastName and age properties. Create two people. Test to see that it's working ok.

// Example
var Car = function(wheels, seats, engines) {
  this.wheels = wheels;
  this.seats = seats;
  this.engines = engines;
};

var myCar = new Car(6, 3, 1);
console.log(myCar);

// Only change code below this line